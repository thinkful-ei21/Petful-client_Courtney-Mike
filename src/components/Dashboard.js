import React from 'react';

export default function Dashboard(props) {

  return (
    <div>
      {props.catToAdopt}
      {props.dogToAdopt}

      <header>
        <h1>{name}</h1>
        <img src={} alt={}/>
      </header>
      <main>
        <dl>
          <
        </dl>
      </main>
    </div>
  );
}
